# Browser-side variables exposed via `process.env.NEXT_PUBLIC_*`. The Next.js
# dev server only exposes variables prefixed with `NEXT_PUBLIC_` to client code.
# Copy this file to .env.local (gitignored) and fill in real values.

# URL the browser POSTs to in order to create a ScanUpload session.
# Same-origin path — Next.js rewrites `/hub-api/*` to the real hub URL via
# `rewrites()` in next.config.ts, so the browser sees a same-origin POST
# (no CORS, no CSP complications).
NEXT_PUBLIC_SESSION_URL=/hub-api/api/v2/front-end/session

# Where the `/hub-api` rewrite points to. Set at build time (or via the
# `NEXT_PUBLIC_HUB_API_TARGET` env var when running `next start`) to the
# real hub base URL with no trailing slash.
NEXT_PUBLIC_HUB_API_TARGET=https://hub.scanupload.net

# Tenant / Keycloak `client_id`. Sent in the request body as
# `{ "clientId": "..." }` so the hub can scope audit, rate-limits, and
# per-client rules. Optional — leave blank to omit.
NEXT_PUBLIC_CLIENT_ID=